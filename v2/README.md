# V2 comparison

This is an alternate presentation design based on the existing AdaL slide visual language from `ai-girl-glow`.

It does not replace the original pages in the repository root.

Open `index.html` to compare the v2 course home, then open the decks under `slides/`.
